/**
 * Advanced Form Integration - Action Components (DEPRECATED STUB)
 *
 * The contents of this file have been split into per-platform component
 * files at:
 *
 *     platforms/<provider>/<provider>-component.js
 *
 * They are loaded on demand by `adfoinComponentLoader.loadPlatform(name)`
 * (see assets/js/core.js). Shared `adfoinHelpers` were also moved into
 * core.js so they are available before any platform component executes.
 *
 * This stub remains so the legacy `adfoin-main-script` handle registered
 * by `wp_register_script()` still resolves for any third-party code that
 * explicitly enqueues it. New code should depend on `adfoin-app` and call
 * `adfoinComponentLoader.loadPlatform(name)` instead.
 */
